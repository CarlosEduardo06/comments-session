export default ({children}) => {
    return (
        <div className="container-card">
            <h2>
                Seção comentários
            </h2>
            {children}
        </div>
    )
}